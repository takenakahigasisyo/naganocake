<div class='container'>
<h2><%= @customer_name %>さんの会員詳細</h2>


<table class='table table-borderless'>
  <tr>
    <th scope="col" style="width: 30%">会員ID</th><td><%= @customer.id %></td>
  </tr>
  <tr>
    <th>氏名</th><td><%= @customer.last_name %>  <%= @customer.first_name %></td>
  </tr>
  <tr>
    <th>フリガナ</th><td><%= @customer.last_name_furigana %>  <%= @customer.first_name_furigana %></td>
  </tr>
  <tr>
    <th>郵便番号</th><td><%= @customer.postcode %></td>
  </tr>
  <tr>
    <th>住所</th><td><%= @customer.address %></td>
  </tr>
  <tr>
    <th>電話番号</th><td><%= @customer.phone_number %></td>
  </tr>
  <tr>
    <th>メールアドレス</th><td><%= @customer.email %></td>
  </tr>
  <tr>
    <th>会員ステータス</th>
    <td>
      <% if @customer.is_active %>
        <p class="text-success">有効</p>
      <% else %>
        <p class="text-secondary">退会</p>
      <% end %>
    </td>
  </tr>
</table>

<%= link_to '編集する', edit_admin_customer_path(@customer.id), class: 'btn btn-outline-dark' %>
<%= link_to '注文履歴一覧を見る', root_path, class: 'btn btn-outline-dark' %>
   
</div>

   