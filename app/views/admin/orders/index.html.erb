<div class="container py-5">
  <div class="row ">
    <div class="col-md-3">
      <h2>注文履歴一覧</h2>
    </div>
      <!--<div class="offset-md-8">-->
      <!--  <　%= link_to new_admin_item_path,class: 'nav-link',style: 'border-radius: 50%; box-shadow: 2px 2px 2px 0 rgba(0, 0, 0, 0.5)' do %>-->
      <!--    <i class="fa-regular fa-plus"></i>-->
      <!--  <　% end %>-->
      <!--</div>-->
    </div>
    <br>
    <table class="table thead-light">
      <thead class="thead-light">
      <tr>
        <th>購入日時</th>
        <th>購入者</th>
        <th>注文個数</th>
        <th>注文ステータス</th>
      </tr>
      </thead>
      <% @orders.each do |order| %>
      <tr>
        <td><%= link_to "#{order.created_at.strftime('%Y/%m/%d %H:%M:%2N')}", admin_order_path(order.id)  %></td>
        <td><%= order.customer.last_name %><%= order.customer.first_name %></td>
        <!--<td>< %= link_to "#{item.name}", admin_item_path(item.id) %></td>-->
        <td><%= order.order_details.sum(:amount) %></td>
        <td><%= order.status_i18n %></td>
        <!--<td>-->
        <!--< % if item.is_saled %>-->
        <!--  <p style="color: red;">販売中</p>-->
        <!--< % else %>-->
        <!--  <p style="color: blue;">販売停止中</p>-->
        <!--< % end %>-->
        <!--</td>-->
        <!--<td><　%= item.is_saled ? '販売中' : '販売停止中' %></td>-->
      </tr>
      <% end %>
    </table>

  <div class="item__paginate"><%= paginate @orders %></div>
</div>